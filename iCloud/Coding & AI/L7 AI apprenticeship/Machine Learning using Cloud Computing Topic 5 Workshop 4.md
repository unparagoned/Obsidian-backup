Might need to do a bit of own research into platforms on the assignment.

RNN were used before LLM, LLM are a type of autoencoder

Autoencoders learn efficient encodings of data

Have an encoding component and a decoding component

CNN and RNN were mainly used, then there was a fast turnaround to autoencoders/transformers

Filter and pools for a number of layers, then vectorised and passed to a NN

Region in the input image is called local receptive field

Receptive field, kernel and filter are used interchangeably

Typically you have two layers of feature maps and pooling

Flattened then CNN connects to a fully connected feed forward network

Adam based on stochastic gradient descent

Vanishing gradients mitigations

- Relu rather than tanh
- Initialisation f W matrix
- Aggressive regularisation
- Pretraining the layers using unsupervised methods